services:
  postgres:
    image: postgres:15-alpine
    container_name: nginx-warden-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-nginx_warden_db}
      POSTGRES_USER: ${DB_USER:-nginx_warden_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-change_this_password}
    ports:
      # Bind only to localhost for security (matches deploy.sh)
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    volumes:
      # Use consistent volume name with deploy.sh
      - nginx-warden-postgres-data:/var/lib/postgresql/data
    networks:
      - nginx-warden-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-nginx_warden_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  nginx-warden-postgres-data:
    driver: local

networks:
  nginx-warden-network:
    driver: bridge